# Examples

- `ecg_like.py` - synthetic ECG-like signal that exercises generators,
  filtering, framing, feature extraction, PSD, and spectrogram.

Run with the package installed:

```bash
pip install -e .
python examples/ecg_like.py
```
